const { signIn } = require('./signIn');
const { setAddress } = require('./setAddress');
const { getUserById } = require('./getUserById');

module.exports = {
    signIn,
    setAddress,
    getUserById,
};
